<form {{action "save" on="submit"}}><% attrs.forEach(function(attr) { %>
  <div>
    <label>
      <%= attr.label %>
      {{input type="text" value=model.<%= attr.name %>}}
    </label>
  </div><% }); %>
  <div>
    <input type="submit" value="Save">
  </div>
</form>
