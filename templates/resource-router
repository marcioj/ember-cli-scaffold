  this.resource('<%= resource.plural %>', function() {
    this.route('new');
    this.route('edit', { path: ':<%= resource.singular %>_id/edit' });
    this.route('show', { path: ':<%= resource.singular %>_id' });
  });
